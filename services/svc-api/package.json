{
  "name": "@shush/svc-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --enable-source-maps dist/index.js",
    "build": "tsc -b",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "generate:openapi": "pnpm run build && node --enable-source-maps dist/scripts/generate-openapi.js"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.1",
    "@hono/standard-validator": "^0.2.0",
    "@valibot/to-json-schema": "^1.6.0",
    "hono": "^4.9.7",
    "hono-openapi": "^1.3.0",
    "valibot": "^1.1.0"
  }
}
